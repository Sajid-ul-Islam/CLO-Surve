# Copy this file to .streamlit/secrets.toml for local dev, OR
# paste these keys into Streamlit Cloud → App settings → Secrets.
# (.streamlit/secrets.toml is gitignored — never commit real secrets.)

OPENROUTER_API_KEY = "sk-or-v1-..."
CLO_MODEL = "z-ai/glm-5.3-flash"
